# 关键：跨平台 node_modules 不能进容器，rollup/esbuild 等是平台特定二进制
# 容器构建会自己跑 npm install，重新拉对的平台版本
node_modules/

# 已构建产物（容器里会重新 build）
dist/
.vite/

# 日志
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# 敏感
.env
.env.local

# Git
.git/
.gitignore

# IDE
.vscode/
.idea/
*.swp
